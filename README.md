# C++ Builder 13.1 lacks C++23 support (Demo project)

Try and compile this with bcc64x in C++ Builder Florence.

- Language features seem to be there (deducing this)
- Standard library is absent (generator, cartesian_product)
